# 服务器记录

## 当前项目服务器

- 公网地址：171.80.15.112
- SSH 端口：30213
- SSH 用户：root
- SSH 密码：不在项目文档中保存
- 主机名：ts26026792
- 系统：Ubuntu 24.04 LTS
- 项目目录：`/opt/ikun-aishare-api`
- 代码仓库：<https://github.com/lix664100-sudo/-API>
- 代码分支：`main`

## 公网访问

- 后台地址：<https://lixiang.9991.site/admin/>
- 健康检查：<https://lixiang.9991.site/api/health>
- HTTPS 转发：Nginx
- Nginx 配置：`/etc/nginx/sites-enabled/lixiang.9991.site`
- Nginx 转发目标：`http://127.0.0.1:3210`

项目服务只监听服务器本机的 `127.0.0.1:3210`，公网请求先进入 Nginx，再转发给项目服务。

## 服务运行方式

- 运行用户：ubuntu
- 服务名称：`ikun-api`
- 启动文件：`/opt/ikun-aishare-api/src/server.js`
- 工作目录：`/opt/ikun-aishare-api`
- Node.js：20.20.0
- 运行方式：PM2 单进程
- PM2 开机启动服务：`pm2-ubuntu.service`
- PM2 数据目录：`/home/ubuntu/.pm2`
- 普通日志：`/home/ubuntu/.pm2/logs/ikun-api-out.log`
- 错误日志：`/home/ubuntu/.pm2/logs/ikun-api-error.log`

查看服务状态：

```bash
sudo -u ubuntu env PM2_HOME=/home/ubuntu/.pm2 pm2 status
```

查看服务日志：

```bash
sudo -u ubuntu env PM2_HOME=/home/ubuntu/.pm2 pm2 logs ikun-api
```

重启服务：

```bash
sudo -u ubuntu env PM2_HOME=/home/ubuntu/.pm2 pm2 restart ikun-api --update-env
```

## 数据位置

- 任务和账号数据：`/opt/ikun-aishare-api/data`
- 图片入口：`/opt/ikun-aishare-api/outputs`
- 图片实际目录：`/data/ikun-api/outputs`

`/opt/ikun-aishare-api/outputs` 是指向 `/data/ikun-api/outputs` 的链接。

## 代码更新状态

- 本地、GitHub 和服务器均使用 `main` 分支
- 服务器代码已与 GitHub 同步
- 网页更新命令已使用正确的服务名称 `ikun-api`
- 同步前的服务器代码已保留在 Git 暂存记录 `pre-sync-20260727`
- 同步前的代码、设置和数据备份：`/opt/ikun-aishare-backups/sync-20260727-JsaCVJ`

## 当前需要注意

### GitHub 更新使用账号代理

服务器直连 GitHub 和 npm 官方下载站不稳定。账号代理访问 GitHub 的实测时间约为 1.5 到 1.8 秒，npm 国内源的实测时间约为 0.6 秒。

新版更新流程上线后会按以下规则处理：

1. 只使用已启用、格式正确且未到期的账号代理。
2. 相同代理只尝试一次，避免多个账号共用代理时重复等待。
3. 每次更新随机排列可用代理；连接失败后自动尝试下一条。
4. GitHub 版本检查和正式拉取代码使用同一条已验证代理。
5. npm 使用国内下载源，不占用账号代理。
6. 所有代理都失败时停止更新，不修改代码，也不重启服务。
7. 后台提示和日志不显示代理账号或密码。

检查时共有 3 个启用账号配置了代理，去重后是 2 条代理，均在 2026-08-10 到期。代理到期后需要先更换账号代理，才能继续使用自动更新。

### 端口以实际运行状态为准

服务器 `.env` 中仍写有 `PORT=80` 和 `HOST=0.0.0.0`，但当前 PM2 实际使用的是 `PORT=3210` 和 `HOST=127.0.0.1`。排查访问问题时，以 PM2 的实际运行状态和 Nginx 配置为准。

## 历史服务器

- 公网地址：124.222.173.31
- SSH 端口：22
- SSH 用户：ubuntu
- SSH 密码：不在项目文档中保存
- 系统：Ubuntu 24.04.4 LTS
- 内网 IP：10.0.0.7
- 项目目录：`/opt/ikun-aishare-api`
- 后台端口：80
- 后台公网地址：<http://124.222.173.31/admin/>
- 后台账号和密码：不在项目文档中保存

历史服务器是否仍承担业务需要另行确认，不要把它当作当前项目服务器操作。

## 操作原则

不要直接修改服务器上的项目文件，除非已经明确获得同意。正常处理方式是先修改并测试本地代码，再通过 Git 更新服务器。
